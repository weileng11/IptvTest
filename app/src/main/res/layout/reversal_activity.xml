<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/id_container"
    >

    <ImageView
        android:id="@+id/id_ball"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@mipmap/bg_science"
        android:scaleType="centerCrop"
        android:onClick="rotateyAnimRun"
        />

    <ImageView
        android:id="@+id/img_alph"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/id_ball"
        android:layout_centerInParent="true"
        android:layout_marginBottom="15dp"
        android:src="@mipmap/bg_story"
        android:scaleType="centerCrop"
        android:onClick="propertyValuesHolder"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/id_ball"
        android:layout_marginBottom="15dp"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:onClick="verticalRun"
        />
</RelativeLayout>
